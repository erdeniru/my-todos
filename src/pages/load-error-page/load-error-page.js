import { BackButton } from '../../components';

export const LoadErrorPage = () => {
    return (
        <div>
            <BackButton />
            Не удалось загрузить данные
        </div>
    );
};
