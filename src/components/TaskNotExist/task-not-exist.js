import { BackButton } from '../BackButton/back-button';

export const TaskNotExist = () => {
    return (
        <div>
            <BackButton />
            Задание не сушествует
        </div>
    );
};
